# Monitoring

`GET /api/monitoring/model` reports actual persisted prediction count, average probability, high-risk percentage, model version, and availability status.

Drift and real-world model performance remain foundations. A valid baseline period and future outcome labels are required before declaring drift or deployed degradation.
