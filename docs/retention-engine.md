# Retention Engine

Retention priority is separate from model risk. The documented score combines bounded components for churn probability, customer value, revenue at risk, and engagement proxy.

Business signals are generated only when the source contains the required fields. Recommendations are rule-based suggestions and never guarantee retention. Retention actions persist status and timestamps in PostgreSQL.
